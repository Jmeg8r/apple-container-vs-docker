# Article draft stays local until published to Substack
article/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Raw results are large/noisy; keep curated charts + aggregated stats only.
# Comment the next line out if you want to commit the full raw corpus.
results/raw/*.jsonl

# macOS
.DS_Store

# Run logs are noisy; keep raw jsonl local. Curated stats + charts ARE committed
# (the published N>=10 results, see docs/findings.md).
results/real_run.log
results/iperf_rerun.log
